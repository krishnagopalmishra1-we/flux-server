setuptools>=70.0

# ── Core inference stack ─────────────────────────────
--extra-index-url https://download.pytorch.org/whl/nightly/cu124
torch>=2.4.0
# FlashAttention-2 ships inside PyTorch 2.x via scaled_dot_product_attention.
# Install flash-attn separately only if you want the standalone C++ kernel:
#   pip install flash-attn --no-build-isolation
diffusers==0.35.1
transformers==4.44.2
accelerate==0.33.0
bitsandbytes==0.43.3        # INT8 for T5-XXL text encoder
torchao==0.8.0              # FP8 weight-only quantization for FLUX transformer (faster than NF4)
peft==0.17.0                # LoRA adapter loading via PEFT
safetensors==0.4.4
sentencepiece==0.2.0
protobuf==4.25.4

# ── Web server ───────────────────────────────────────
fastapi==0.111.1
uvicorn[standard]==0.30.6
gunicorn==22.0.0
pydantic-settings==2.4.0
python-multipart==0.0.9

# ── Image processing ─────────────────────────────────
Pillow>=10.0.0
numpy>=1.26.0
scipy>=1.13.0
einops==0.8.0

# ── HuggingFace / misc ───────────────────────────────
huggingface-hub==0.35.0
requests>=2.31.0
pandas>=2.2.0
tqdm==4.66.5
aiofiles==23.2.1
