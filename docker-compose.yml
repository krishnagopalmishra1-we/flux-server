services:
  flux-server:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - hf-cache:/mnt/hf-cache
      - outputs:/mnt/outputs
      - ./loras:/app/loras
    env_file:
      - .env
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: 1
              capabilities: [gpu]
    shm_size: 2gb
    restart: unless-stopped

volumes:
  hf-cache:
  outputs:
